import { Component } from "react";
export default class RoutineManagerErrorBoundary extends Component { constructor(props){super(props);this.state={error:null};} static getDerivedStateFromError(error){return{error};} render(){if(this.state.error)return <main className="routine-shell-centered"><section className="routine-state-card" role="alert"><p className="eyebrow">Preview remains available</p><h1>Manager Control Center stopped</h1><p>No local draft or server data was changed by this rendering error.</p><button type="button" className="primary-button" onClick={this.props.onBack}>Back to preview home</button></section></main>;return this.props.children;} }
