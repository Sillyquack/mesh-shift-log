# Mesh Shift Log production cutover — 14 August 2026

## Windows

1. Primary: night Friday 14 → Saturday 15 August 2026.
2. Fallback: night Saturday 15 → Sunday 16 August 2026.
3. Final fallback: night Sunday 16 → Monday 17 August 2026.
4. Target official use: Monday morning 17 August 2026.

A missed window is safer than an improvised migration. No scope expansion is allowed after review.

## Merge order

1. PR #13 — Julie routines and Event Mode.
2. PR #14 — shared Mesh Experience System and Shift Mode.
3. PR #15 — Inventory Count Mode.
4. PR #16 — role-aware entry, Manager Today/Attention/Control, Visual Standards, History, Production Readiness and Phase 10W.

Record the exact integrated SHA and deploy only the reviewed artifact.

## Hard GO gates

- Bobby approves all frontline wording and order.
- Required images are uploaded with correct alt text and task usage.
- Final full CI is green, including pinned disposable PostgreSQL.
- Full Routine Engine migration reapply is green on `17.6.1.127`.
- Phase 9 full verification is green on `17.6.1.141`.
- Phase 10W static, database, runtime and exact-reapply checks are green.
- Fresh production catalog/ledger preflight still matches.
- Supabase backup and restore availability is manually confirmed immediately before the window.
- Named owners are present for migration, deployment, four-role smoke and forward-fix decisions.

## Database package

Do not run the whole `supabase/` directory and do not replay Phase 9 or Routine Engine A–R.

### 10S–10V

Live catalog evidence already contains their effective outcomes. Treat them as ledger reconciliation or exact idempotent reapply candidates only. Any mismatch is NO-GO.

### 10W

Apply `phase10w_event_operations_authenticated_execute.sql` only after exact preflight passes. It changes function execution boundaries and `set_updated_at()` search path only.

### Content pack

The live provider is 1.4R while the recorded installation is 1.1R. Update through the existing guarded content-pack workflow after Bobby approves content. Keep templates as drafts until publication is separately approved.

## Cutover sequence

1. Freeze manager editing, image replacement, content installation and release changes.
2. Confirm no active Routine Engine runs or counter assignments.
3. Capture ledger, settings, counts, ACLs, provider hash and backup confirmation.
4. Reconcile/reapply only approved S→T→U→V entries if required.
5. Apply 10W.
6. Install reviewed 1.4R without publishing, promoting or changing mode.
7. Verify database health, ledger, counts, ACL matrix and unchanged approved history.
8. Deploy the exact integrated application artifact.
9. Smoke test Manager, Event Floor Manager, staff/shared device and Counter.
10. Keep `shadow`/`staff_preview` until readiness is recomputed and Bobby explicitly approves activation.

## Immediate NO-GO conditions

- unauthorized or cross-organization access;
- lost or falsely confirmed writes;
- broken manager/event operations;
- altered immutable history;
- unexpected row-count or migration-ledger drift;
- missing backup confirmation;
- a different build SHA than the reviewed candidate.

## Forward-fix

- App regression: route back to the previous known-good deployment.
- 10W role regression: restore only the exact required `authenticated` grant; never restore broad `anon`/`PUBLIC` execution.
- Content issue: keep drafts unpublished or publish a corrected new version.
- Image issue: replace through the versioned upload flow or return to placeholder.
- Any uncertain database state: retain `shadow`, stop and use the next window.
